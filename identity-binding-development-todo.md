# 用户身份绑定开发代办

依据：`commit-change-report.md` 中的身份绑定审计。

目标：隐藏礼物、秘密 Gift 页面、STATS 面板和生日信流程均使用 `space_members.role` 判断业务角色，不使用邮箱、显示名或写死的 Auth UUID 判断权限。本代办按上述功能仅向 `mel` 开放编写。

状态说明：`[x]` 已完成并验证；`[ ]` 尚未完成。标注“边界限制”的项目会改变现有显示内容或需要先做产品口径决定，本次按要求未修改。

## 模块一：统一用户身份上下文

- [X] 复用 `get_glimmer_dashboard(space_id)` 返回的 `user_id`、`role`、`display_name` 和空间信息，作为前端唯一的业务身份来源。
- [X] 调整 `initializeGlimmerSession()`，向 `script.js` 暴露只读身份上下文，或新增统一的身份初始化接口。
- [X] 调整 `unlock()`：等待身份上下文加载完成后，再初始化角色相关页面和功能。
- [X] 身份加载期间默认隐藏所有角色专属入口，避免页面闪现。
- [X] 身份加载失败时保持专属功能关闭，并显示可理解的错误；session 失效时沿用统一退出流程。
- [X] 退出登录和切换账号时清空身份上下文、礼物状态、统计弹窗状态及相关内存缓存。
- [X] 提供统一判断方法，例如 `hasRole("mel")` 或 `canAccessFeature(featureId)`，避免各模块重复比较字符串。

## 模块二：隐藏礼物与秘密 Gift 页面

### 前端角色门控

- [X] 仅当当前空间角色为 `mel` 时显示 5 个礼物图标。
- [X] 仅对 `mel` 调用 `loadGiftState()`，Ray 登录时不得发起礼物 RPC。
- [X] `collectGiftIcon()` 执行前再次检查当前角色为 `mel`。
- [X] `applyGiftState()` 同时检查角色和收集状态，不能只检查 `giftIconsFound`。
- [X] Gift 导航仅在“当前角色为 `mel` 且已集齐 5 个礼物”时显示。
- [X] `showPage("gift-secret")` 同时验证 `mel` 角色和集齐状态；校验失败返回首页。
- [X] 身份或礼物状态请求失败时保持礼物功能隐藏，不再使用“默认全部未收集并显示”的降级行为。
- [X] Ray 登录时确认礼物按钮、Gift 导航和秘密页内容均不可访问。

### 后端角色授权与存储

- [X] 在 `get_gift_icons_found()` 中查询当前用户的 `space_members.role`，非 `mel` 调用返回明确的权限错误。
- [X] 在 `collect_gift_icon(text)` 中重复执行相同角色校验，不能只依赖前端隐藏。
- [X] RPC 同时校验用户属于目标 `space_id`，避免未来多空间时发生串空间访问。
- [X] 决定收集进度的生命周期：按 `(space_id, role)` 绑定 Mel 席位，账号重建后继承进度。
  - [X] 新增 `gift_progress` 角色进度表，迁移既有 profile 数据并移除旧列。
- [X] 为礼物 JSONB 增加对象结构与 5 个允许键的数据库约束，RPC 也只写入白名单键。
- [X] 保留礼物 ID 前后端白名单校验。
- [X] 保持 RPC 仅授权 `authenticated`，撤销 `anon` 和 `public` 权限。
- [X] 为角色不匹配或成员不存在定义稳定的 `FEATURE_FORBIDDEN` 错误码。

### 礼物迁移部署

- [X] 修订并审查 `supabase/migrations/202607190001_gift_icons_found.sql`，加入角色与空间校验。
- [ ] 在本地执行数据库重置和迁移，确认从空库可完整创建。（环境阻塞：Docker Desktop 未运行；已改用远端事务测试）
- [X] 将礼物迁移推送到当前远端 Supabase 项目。
- [X] 执行数据库 lint，确认无 warning/error。
- [X] 确认 PostgREST schema cache 已识别两个礼物 RPC。
- [X] 用 Mel 验证查询和收集成功，用 Ray 验证两个 RPC 均被拒绝。

## 模块三：关系数据 STATS 面板

### 前端角色门控

- [X] 默认隐藏 `#statsButton` 和 `#statsModal`。
- [X] 身份上下文确认当前角色为 `mel` 后再显示 STATS 入口。
- [X] `openStatsModal()` 内再次校验 `dashboard.role === "mel"`，防止绕过 UI 直接触发。
- [X] Ray 登录时不注册统计入口操作，或确保操作函数立即拒绝。
- [X] 退出登录、切换账号或身份失效时立即关闭统计弹窗并清空已显示结果。

### 统计计算与数据边界

- [X] 保持统计范围为当前固定共享空间，不混入其他空间数据。
- [X] 明确 4 项指标的现有计算口径并拆为可测试纯函数；未改变显示内容。
- [X] 明确照片数量统计的是 glimmer 条目数还是去重后的图片资产数，并统一界面文案。
  决策：直接统计条目数
- [X] 明确中文分词策略；使用 `Intl.Segmenter` 自然语言分词、基础中英文停用词表及兼容回退。
  决策：添加一个和当前框架适配的自然语言分词服务，并添加一个基础停用词表。
- [X] 为相同词频定义稳定的并列处理规则。
- [X] 确认最长连续记录以空间时区和服务端 `local_today` 为准。
- [X] 处理无数据、部分月份加载失败和 session 过期状态。
- [X] 已评估按月并发模型；当前数据跨度较短，保留现实现，数据年份增长后再改为服务端聚合。

### 安全边界说明

- [X] 在开发文档中明确：STATS 的 Mel-only 限制属于产品/UI 门控。
- [X] 不把 STATS 门控描述为严格的数据保密措施；Ray 已能读取共享微光并自行计算相同统计。
- [X] 如果未来统计包含任一成员原本无权读取的数据，再新增后端专用 RPC 和角色授权。

## 模块四：生日信与开场流程

- [X] 仅当当前空间角色为 `mel` 时显示生日信和点灯开场。
- [X] Ray 登录后直接进入允许访问的主站页面，不显示生日信。
- [X] `preludeStorageKey()` 继续用于保存完成状态，但不能承担角色授权职责。
- [X] 完成状态继续绑定 Mel 的 Auth 用户；角色授权独立由 `space_members.role` 完成。
- [X] `returnLetterButton` 仅对 `mel` 显示和生效。
- [X] 直接调用开场相关函数时再次校验角色。
- [X] 切换账号时重置开场 DOM 状态，避免 Ray 继承 Mel 当前页面状态。

## 模块五：自动化测试

### 前端单元测试

决策：补完测试

- [X] 为统一身份判断增加 `mel`、`ray`、未登录用例；身份加载失败由默认拒绝路径覆盖。
- [X] 测试 Mel 可加载/收集礼物，Ray 不调用礼物 Repository。
- [X] 测试 Gift 页面必须同时满足 Mel 角色与集齐条件。
- [X] 测试 Mel 可打开 STATS，Ray 无入口且直接调用也被拒绝。
- [X] 测试 Mel 进入生日信、Ray 跳过生日信。
- [X] 测试退出和账号切换后角色专属页面回到 Home，UI 与缓存由统一清理路径重置。

### Repository 与数据库测试

- [X] 保留礼物 RPC 参数、返回值和非法 gift ID 测试。
- [X] 增加 Mel 调用礼物查询/收集成功测试。
- [X] 增加 Ray 调用礼物查询/收集返回权限错误测试。
- [X] 增加匿名用户调用两个礼物 RPC 被拒绝测试。
- [X] 增加角色不匹配、profile 缺失和错误空间测试；错误空间同时覆盖非目标空间成员路径。
- [X] 按 `(space_id, role)` 存储，并增加账号重建/角色重新绑定后的进度继承测试。
  决策：允许进行
- [X] 为统计计算添加空数据、日期范围、分词、停用词、并列词频和连续记录中断测试。

### 浏览器双角色测试

- [X] 使用 `localhost` 与 `127.0.0.1` 隔离两个同源 session，同时登录 Mel 和 Ray。
- [X] 通过各自唯一可见的上传栏确认角色识别正确。
- [X] Mel：按业务流程收集 5 个礼物，验证刷新持久化、Gift 导航与秘密页解锁；测试后已将远端进度清空。
  决策：按业务流程实测推进到gift解锁，测试完后再回删
- [X] Ray：验证礼物、Gift 导航和秘密页均不可访问。
- [X] Mel：验证 STATS 可见且指标正确。
- [X] Ray：验证 STATS 不可见且无法通过页面交互打开。
- [X] Mel：验证生日信首次显示、完成后刷新与“回到生日信件”行为正确。
- [X] Ray：验证登录后不进入生日信流程。
- [X] 测试完成后退出两个账号，不保留测试 session 或临时业务数据。

## 模块六：上线前验收

决策：先补完

- [X] 运行 `npm test`，30 项前端/Repository 测试全部通过。
- [X] 运行远端 Supabase 礼物身份事务测试，达到 `ok 9`；整套本地测试因 Docker 未运行未执行。
- [X] 运行远端 migration dry-run，确认只有预期迁移待部署。
- [X] 部署迁移后检查 RPC 签名、授权对象和 schema cache。
- [X] 在当前远端开发项目执行 Mel/Ray 双账号 smoke；礼物临时写入已清理。
- [X] 检查浏览器控制台无礼物 RPC 缺失、权限误判或未处理 Promise 错误。
- [X] 检查窄屏和桌面端角色专属元素不会留下空白占位或布局错位；修复秘密页覆盖 `.page` 隐藏规则的问题。
- [X] 检查退出登录、刷新、token 刷新和 session 过期后的 UI 状态一致；浏览器验证同源退出同步，自动化覆盖 session 过期，Auth 监听覆盖 token 刷新。
- [X] 更新部署记录，写明迁移版本、测试结果、目标角色和已知的 STATS 数据边界。

## 完成标准

- [X] Mel 能完整使用隐藏礼物、Gift 页面、STATS 和生日信流程。
- [X] Ray 只能使用双方共享的常规功能，看不到或无法触发上述 Mel 专属功能。
- [X] 前端函数和后端 RPC 均执行角色复核，Ray 不能收集礼物或解锁 Gift。
- [X] 刷新、双 origin 会话和同 origin 账号切换后，身份判断与状态恢复均符合预期。
- [X] 远端不再出现 `get_gift_icons_found` / `collect_gift_icon` 缺失错误。
