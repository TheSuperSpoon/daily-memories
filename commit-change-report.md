# 两个 Commit 修改简报

## 范围

- 基准提交：`935d95c0aaf93d7625f9a169bff2a04395ce6428`（不含）
- 分析提交：
  1. `cb22c30e87d9c2f02a1452e5604da035584a05b2` — **Add Surprise**
  2. `eb82c16baecbfde78f32535537037387316febf4` — **Add counter**
- 合计：2 个 commit，涉及 7 个文件；新增 657 行、删除 11 行。

## 功能块一：隐藏礼物收集与秘密页面【当前按账号保存，未按角色限制】

来源：`cb22c30`（Add Surprise）

- 在首页、灯塔、相册今日日期、播放列表和票根中加入 5 个低可见度礼物图标。
- 用户点击礼物后，以当前登录用户为单位保存收集状态、隐藏已收集图标，并通过 Toast 显示当前进度。
- 集齐 5 个礼物后为该用户解锁第 6 个导航入口 **Gift**；未集齐时直接访问秘密页会被送回首页。
- 新增秘密礼物占位页，后续可替换为歌曲、语音或专属内容。
- 相册页新增当天日期展示，并由浏览器动态填充日期。

## 功能块二：礼物状态持久化与访问控制【当前按 Auth 用户绑定，未绑定 `mel` 角色】

来源：`cb22c30`（Add Surprise）

- `profiles` 表新增 `gift_icons_found jsonb` 字段，用于按用户保存收集进度；不同账号拥有各自独立的礼物状态。
- 新增 `get_gift_icons_found()` 和 `collect_gift_icon(text)` 两个 Supabase RPC。
- RPC 仅授权给已登录用户，并通过 `auth.uid()` 限定当前用户；前后端均校验礼物 ID 白名单。
- 登录后加载服务端礼物状态，退出登录时清空页面内状态，避免不同账号间串数据。
- 开场动画完成标记由全局 localStorage 键改为按用户 ID 隔离的键，即同一浏览器上的不同账号不会共用该完成状态。

## 功能块三：关系数据统计面板【当前对所有空间成员开放，未按角色限制】

来源：`eb82c16`（Add counter）

- 微光页头部新增 **STATS / Us in numbers** 入口和统计弹窗。
- 自动计算并展示 4 项指标：
  - 相识天数：从固定日期 `2026-03-29` 起按自然日包含首尾计算；
  - 照片数量：统计微光记录总数；
  - 最常用词：从 caption 中提取英文单词或连续中文词组，并过滤内置停用词；
  - 最长连续记录：统计双方都完成上传的最长连续天数。
- 打开弹窗时按月加载当前登录用户所属共享空间中、从微光起始日到今天的数据；计算中、成功和失败均提供状态提示。
- 统计弹窗接入现有统一关闭逻辑。

> 身份边界：统计结果来自登录会话可访问的共享微光数据，因此依赖用户身份和空间成员关系；但结果是关系/空间级统计，不是像礼物进度、开场动画标记那样的个人专属状态。

## 功能块四：视觉与响应式适配

来源：`cb22c30`、`eb82c16`

- 为隐藏礼物补充低透明度、悬停/键盘聚焦反馈、移动端周期提示、Toast 和秘密页样式。
- 为相册今日日期卡片、统计入口和统计卡片补充统一的紫色主题样式。
- 统计卡片桌面端为双列，窄屏下切换为单列。

## 功能块五：测试覆盖

来源：`cb22c30`（Add Surprise）

- 新增 Repository 测试，覆盖礼物状态查询、礼物收集 RPC 参数及返回值。
- 新增非法礼物 ID 的前端拦截测试，确认不会发起 RPC。
- `eb82c16` 的统计计算暂未新增自动化测试。

## 用户身份绑定需求与实现审计

### 审计口径

系统中应作为功能授权依据的是 `space_members.role`，当前只有两个业务角色：`ray` 和 `mel`。Auth 用户 UUID 只用于识别某一个登录账号，邮箱和显示名也不应作为角色判断依据。

以下按两个新增功能均应只对 **`mel` 角色**开放进行审计。隐藏礼物秘密页明确写有“只给 Mel”，整个入口页也标注为 “For Mel”；关系统计作为本次新增的展示型惊喜，按相同目标角色处理。如果产品最终要求 STATS 只对 `ray` 开放，根因和修复结构不变，只需把允许角色从 `mel` 改为 `ray`。

后端其实已经能正确解析角色：`get_glimmer_dashboard()` 根据 `auth.uid()` 查询 `space_members`，并在返回值中包含 `role`。现有上传功能也使用 `dashboard.role` 正确区分 Ray/Mel，因此本次故障不是账号角色分配错误，而是两个新功能没有接入已有角色上下文。

## 身份绑定功能一：隐藏礼物收集与秘密 Gift 页面

### 需求逻辑：要和谁绑定

- **目标角色：`mel`。** 礼物寻找、收集进度、解锁导航和秘密页面均是给 Mel 的惊喜体验。
- 判断依据必须是当前登录用户在固定共享空间中的 `space_members.role = 'mel'`，不能依赖邮箱、注册顺序在前端的推断、显示名或写死的用户 UUID。
- Ray 登录后不应看到或操作 5 个礼物图标，不应加载/写入礼物收集状态，也不应进入秘密页面。
- Mel 的收集进度需要跨设备保存。若 Mel 的 Auth 账号被重建但仍重新获得 `mel` 角色，产品上应明确是否继承旧进度；若要求按角色继承，状态应绑定 `(space_id, role)`，而不是永久绑定旧 `user_id`。
- 前端隐藏只能保证体验，后端 RPC 还必须验证调用者确实是目标角色，避免 Ray 直接调用接口收集礼物。

### 实现逻辑：当前如何和角色绑定

当前实现实际上**没有和 `mel` 角色绑定**，只做了以下账号级处理：

1. 登录成功后，`unlock()` 对任何登录用户都会调用 `loadGiftState()`，没有先等待或读取 `dashboard.role`。
2. `applyGiftState()` 只依据 `giftIconsFound` 决定图标与 Gift 导航是否隐藏，不检查角色。
3. `collectGiftIcon()` 只检查礼物 ID 是否在白名单及是否已收集，不检查当前角色。
4. 数据库把进度存入 `profiles.gift_icons_found`；两个 RPC 都通过 `auth.uid()` 读写当前用户的 profile。这实现的是“每个 Auth 用户一份状态”，不是“只有 Mel 可使用”。
5. RPC 权限授予整个 `authenticated` 角色，SQL 内没有查询 `space_members.role`，因此一旦迁移部署，Ray 也能合法调用。
6. 5 个礼物按钮和秘密页 DOM 对两个角色都会渲染；秘密页的前端保护仅检查是否集齐礼物，不检查访问者是不是 Mel。

相关位置：`script.js:156-205`、`script.js:225-229`、`js/glimmer-repository.js:96-108`、`supabase/migrations/202607190001_gift_icons_found.sql:4-35`。

### 实现审计：为什么实测没有绑定到预期角色

主要原因有三层：

1. **角色上下文断链。** `dashboard.role` 位于 `glimmer-controller.js` 的模块私有状态中，`script.js` 的礼物功能无法读取；`initializeGlimmerSession()` 又是异步触发且没有在 `unlock()` 中等待完成，礼物逻辑因此退化为“有 session 就加载”。
2. **前后端都缺少角色授权。** 前端没有 `role === 'mel'` 门控，数据库 RPC 也只验证 `auth.uid()` 对应 profile 是否存在。
3. **远端迁移未部署。** localhost 连接当前远端 Supabase 实测时，Mel 和 Ray 都报错：`Could not find the function public.get_gift_icons_found ... in the schema cache`。加载失败后异常被捕获，页面调用 `applyGiftState()` 保留默认的“5 个均未收集”状态，结果是两个角色都能看到全部礼物，而且点击后无法持久化。

建议的绑定链路：

```text
Auth session.user.id
  → get_glimmer_dashboard(space_id)
  → space_members.role
  → role === "mel"
  → 显示礼物 / 允许加载与收集 / 允许进入秘密页
  → RPC 再次校验 space_members.role = "mel"
```

前端应默认隐藏礼物功能，身份上下文返回并确认 `mel` 后再显示，避免角色信息加载期间短暂露出。后端可在现有 RPC 中加入成员角色校验；若进度属于角色而不是具体账号，建议改为按 `(space_id, role)` 存储。

## 身份绑定功能二：关系数据 STATS 面板

### 需求逻辑：要和谁绑定

- **目标角色：`mel`（按本报告审计口径）。** 只有 Mel 应看到 STATS 入口并打开关系数据弹窗。
- 绑定依据同样应是当前空间中的 `space_members.role`，而不是用户邮箱、显示名或 UUID。
- Ray 登录后应看不到 STATS 按钮，也不能通过直接触发前端函数打开面板。
- 统计内容属于双方共享空间的数据；“只给 Mel 显示”主要是产品体验门控，并不天然构成数据保密，因为 Ray 本来就有权限查看共享微光并可自行计算相同结果。

### 实现逻辑：当前如何和角色绑定

当前实现只绑定到了“已登录且是共享空间成员”，没有绑定到具体角色：

1. `index.html` 无条件渲染 `#statsButton` 和 `#statsModal`。
2. `glimmer-controller.js` 无条件给按钮注册 `openStatsModal` 点击事件。
3. `openStatsModal()` 只检查 `dashboard` 是否存在；任何能成功调用 `get_glimmer_dashboard()` 的空间成员都满足条件。
4. `collectStatsData()` 按月调用 `listGlimmers()`，传入固定 `spaceId`，没有按当前用户或角色过滤，因此 Ray 与 Mel读取的是同一份共享空间数据。
5. 代码虽然已经持有 `dashboard.role`，并在上传栏中正确使用它，但 STATS 的渲染和打开逻辑没有复用这个判断。

相关位置：`index.html:212-380`、`js/glimmer-controller.js:478-529`、`js/glimmer-controller.js:779`。

### 实现审计：为什么实测没有绑定到预期角色

根因是功能实现时只复用了“空间成员可读数据”的能力，没有增加“允许角色可见”的产品规则：

- Mel 页面只有 `mel` 上传按钮可见，Ray 页面只有 `ray` 上传按钮可见，证明 `dashboard.role` 返回正确。
- 但两个页面的 STATS 按钮都可见且都能打开。
- 两边实测结果完全一致：相爱天数 `113`、照片数 `0`、最常用词 `-`、最长连续记录 `0`。这与代码按共享空间汇总的逻辑一致。
- 当前没有 STATS 专用后端 RPC 或角色校验；所有计算都发生在浏览器中。

建议在统计入口渲染和 `openStatsModal()` 中同时检查允许角色，并把角色判定集中到统一身份上下文中。需要注意：由于双方本来都能读取共享微光，后端即使新增 Mel-only 统计 RPC，也只能限制该 RPC，无法阻止 Ray 基于已有明细自行计算；因此这里应定义为 UI/产品体验绑定，而不是安全保密边界。

## localhost + Chrome 双角色实测记录

测试日期：2026-07-19。前端由 `http://localhost:4173` 与 `http://127.0.0.1:4173` 分别打开，以隔离同源 session；两个页面连接相同的远端 Supabase 项目。测试未修改账号密码，未点击收集礼物，未写入线上业务数据。

| 测试账号 | 角色识别证据 | 5 个礼物入口 | STATS 入口 | STATS 结果 |
| --- | --- | --- | --- | --- |
| Mel | 仅 `data-upload-person="mel"` 的上传入口可见 | 可见 | 可见、可打开 | `113 / 0 / - / 0` |
| Ray | 仅 `data-upload-person="ray"` 的上传入口可见 | 可见 | 可见、可打开 | `113 / 0 / - / 0` |

审计判定：**两个功能均未实现目标角色门控。** 账号到 `ray` / `mel` 的映射正常；缺陷位于新功能没有消费角色信息，而不是 Supabase 分配了错误角色。

### 附带发现：生日信/开场流程也不是角色门控

`preludeStorageKey()` 使用 `melPreludeComplete:${authenticatedSession.user.id}`。这只能让不同 Auth 用户拥有不同的本地完成标记，不能保证只有 Mel 能看到生日信。实测 Ray 登录后同样进入完整生日信流程并可完成点灯。若生日信也应只给 Mel，需要使用同一套 `space_members.role = 'mel'` 门控；localStorage 的用户 ID 后缀只能继续用于保存完成状态，不能承担授权职责。

## 部署与维护提示

- 当前远端项目尚未提供礼物 RPC。上线隐藏礼物功能前，需要执行迁移：`supabase/migrations/202607190001_gift_icons_found.sql`，并刷新/等待 PostgREST schema cache 生效。
- 秘密礼物页目前仍是占位内容。
- 关系起始日 `2026-03-29` 和微光默认起始日 `2026-07-20` 写在前端配置/控制器中，日期变化时需同步修改。
- 统计面板会并行读取起始日至今的所有月份；随着数据年份增长，建议关注请求数量和首次打开耗时。

## 当前身份绑定状态汇总

| 功能/状态 | 当前实际绑定 | 预期绑定 | 审计结果 |
| --- | --- | --- | --- |
| 隐藏礼物收集进度 | `profiles.gift_icons_found` + `auth.uid()`；远端 RPC 当前缺失 | 当前空间的 `mel` 角色 | 未实现角色绑定，且暂时无法持久化 |
| 秘密 Gift 导航与页面解锁 | 当前账号是否集齐礼物 | 当前空间的 `mel` 角色 + 集齐状态 | 缺少角色校验 |
| 关系统计面板 | 任意已登录空间成员 | 当前空间的 `mel` 角色 | Ray/Mel 均可见且结果相同 |
| 开场动画完成标记 | localStorage 键包含当前用户 UUID | Mel 可见；完成状态再按账号或角色保存 | 只隔离状态，没有限制角色 |
