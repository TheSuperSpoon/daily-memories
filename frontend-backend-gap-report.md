# 前端接入后端明确约束清单

> 总原则：保留现有页面的紫色星空、灯塔、日历、画廊、弹窗、动效和文案层级；只在现有组件中补充必要状态。账号、权限、日期、上传、删除、奖励和数据持久化全部以后端为唯一真值，前端不得自行放宽规则。

## 1. 前端待接入后端的实现

- [ ] **接入 Supabase 浏览器客户端**
  - 明确约束：不改变现有静态 HTML/CSS 技术栈；安装 `@supabase/supabase-js`，不引入 Next.js 或 `@supabase/ssr`。项目 URL、publishable key 和固定 space ID 通过公开运行时配置注入；禁止在前端、日志或仓库出现 secret/service-role key。

- [ ] **初始化数据层**
  - 明确约束：保持现有 `script.js` 负责页面展示，但所有 Auth/微光操作改为调用 `GlimmerRepository`；在独立启动模块中组装 Supabase client、Storage adapter 和 factory。任何 UI 函数不得出现 `.storage.from(...)` 或直接调用业务 RPC。

- [ ] **注册页面**
  - 明确约束：新增注册/登录界面，视觉沿用现有 gate 面板、输入框、按钮和错误文案风格；注册字段固定为邮箱、显示名、密码、确认密码。提交只调用 `repository.signUp()`；邮箱仅作为账号标识和未来密码重置收件地址，注册后不发送确认邮件、不等待邮箱验证，可直接登录。双人上限和角色分配只以后端结果为准。

- [ ] **登录与 session**
  - 明确约束：进入网站时必须先显示注册/登录界面；只有恢复到有效 session 后才进入现有生日站点并加载微光。保留现有入场动画，但不能再把客户端固定密码视为账号认证。监听 Auth 状态，退出或 session 失效立即清空内存业务数据并返回登录界面。

- [ ] **忘记/重置密码**
  - 明确约束：登录卡片提供“忘记密码”；输入注册邮箱后调用 `resetPasswordForEmail()`，仅该流程使用 Supabase 默认邮件服务。恢复链接进入公开的新密码页面，识别 `PASSWORD_RECOVERY` 后调用 `updatePassword()`；成功后退出恢复 session，并要求使用新密码登录。统一提示“若邮箱存在将收到邮件”，不得暴露账号是否存在。

- [ ] **替换微光数据源**
  - 明确约束：保留现有日历、灯塔、上传栏、画廊和成长展示；删除 `glimmerData`、`retroCards`、`rewardedTens` 的 `localStorage` 持久化。页面数据只由 `listGlimmers()` 返回的 ready/未删除记录生成，刷新和换设备后必须得到相同结果。

- [ ] **按账号角色展示上传入口**
  - 明确约束：移除 Ray/Mel 下拉框；后端 profile/space member role 决定身份。每日面板只显示当前账号自己的上传按钮，另一栏仅显示对方状态和图片；不得通过 DOM、URL 或本地变量切换角色。

- [ ] **接入图片上传状态机**
  - 明确约束：沿用现有上传弹窗、文件选择和 note 输入；提交后在原按钮/弹窗展示“校验中→上传中→确认中→完成”。统一调用 `uploadGlimmer()`，严格执行 begin→StoragePort.upload→finalize；未 finalize 的图片不得进入日历、灯塔或画廊完成态。

- [ ] **上传失败恢复**
  - 明确约束：保持用户停留在当前上传弹窗，不因失败刷新或丢失 note；断网显示重试，上传失败提示已清理 pending，finalize 失败保存 `glimmerId` 并提供“继续确认”按钮调用 `retryFinalize()`。禁止把失败项伪装成已完成。

- [ ] **按需读取私有图片**
  - 明确约束：现有 `<img>` 布局、裁剪和画廊样式不变；图片进入可视区域时才调用 `getImageUrl()`。signed URL 只保存在内存，过期或 401/403 时重新生成；不得写入数据库、HTML 静态内容或 `localStorage`。

- [ ] **列表分页与日期查询**
  - 明确约束：打开微光页先加载当前月和当前选中日期，切月时按对应日期区间查询；画廊沿用现有卡片布局并用 cursor 追加“加载更多”。单次 limit 不超过 100，禁止启动时下载全年原图。

- [ ] **接入删除与替换流程**
  - 明确约束：在现有图片卡片上仅为 owner 且仍在 24 小时窗口内显示删除按钮。删除调用 Storage remove→`complete_glimmer_delete`。若已上传后再次点击上传，弹出沿用现有 modal 风格的确认框；确认后严格执行“删除旧对象并完成删除→重新 begin/upload/finalize”，任一步失败都停止，不允许 Storage upsert 覆盖。

- [ ] **接入奖励账本**
  - 明确约束：背包、卡数、连续天数和成长视觉保持现状；删除客户端发卡和扣卡计算。正常上传完成后调用 `grantStreakRewards()`，补签两人都 ready 后调用 `completeRetroGlimmer()`；界面只展示服务端余额和结果，不允许前端传 amount、milestone 或修改账本。

- [ ] **统一错误提示**
  - 明确约束：沿用现有表单提示、弹窗和按钮禁用样式，不直接弹 SDK JSON。至少分别展示：名额已满、每日重复、文件类型/大小错误、删除超时、余额不足、断网、session 过期和未知错误；session 过期必须返回登录界面，其余错误保留当前操作上下文供重试。

- [ ] **安全渲染 note**
  - 明确约束：note 的字体、位置和换行效果保持现状；实现必须使用 `textContent`、`createElement` 等 DOM API。禁止把 note、邮箱、显示名或后端错误字符串拼入 `innerHTML`；测试 `<script>`、事件属性和 HTML 标签只能显示为纯文本。

- [ ] **生产 Auth 配置**
  - 明确约束：开发期继续使用 localhost；发布前必须填写唯一正式 origin，并同步配置 Site URL 和密码恢复页面 redirect URL，不得使用通配 redirect。本期使用 Supabase 默认邮件服务且仅发送密码重置邮件；接受其约 2 封/小时限制，不配置自定义 SMTP。

## 2. 前端相比后端缺失的实现

- [ ] **真实账号体系 UI**
  - 明确约束：使用当前 gate 的全屏私密入口视觉重做为登录/注册双状态卡片；不新增风格割裂的管理页。固定客户端密码只能保留为纯展示彩蛋门槛，不能替代 Supabase Auth，也不能控制微光数据权限。

- [ ] **邮箱确认流程**
  - 明确约束：不实现注册邮箱确认、等待确认或重发确认邮件；后端关闭 signup email confirmation。邮箱仍必须填写并记录，登录使用邮箱+密码；邮箱真实性只在用户主动重置密码、能够收到恢复邮件时验证。

- [ ] **session 生命周期处理**
  - 明确约束：页面启动先显示 gate/loading，再调用 `getSession()`；恢复成功后继续现有入场流程。跨标签页登录/退出通过 Auth 状态监听同步；过期时销毁 signed URL 和业务内存，提示后回到登录卡片。

- [ ] **云端微光同步**
  - 明确约束：前端按可公开部署的静态站点实现，不依赖本机文件或本地数据库；业务数据全部来自在线 Supabase。`localStorage` 仅允许保存无安全影响的纯 UI 偏好，不得保存图片、note、角色、奖励或完成状态。

- [ ] **私有 Storage 图片**
  - 明确约束：保留现有文件选择和预览外观；本地预览可用临时 object URL，但提交必须上传原始 `File` 到 StoragePort。禁止 `FileReader.readAsDataURL()` 结果进入业务数据或长期缓存；页面卸载时释放临时 object URL。

- [ ] **移除上传人下拉框**
  - 明确约束：彻底删除 `uploaderSelect` 及相关分支，不只是隐藏 CSS。按钮文案由已登录角色生成，例如“上传 Ray 的微光”；后端返回角色前按钮保持禁用，角色未知时不得默认 Ray 或 Mel。

- [ ] **服务端日期约束反馈**
  - 明确约束：日历视觉和本地选日交互保持不变，但可上传性最终以后端为准。前端必须处理 Asia/Shanghai 当天、未来日期、重复上传、补签日期和余额错误；修改浏览器时钟不得解锁后端不允许的日期。

- [ ] **异步加载状态**
  - 明确约束：不重排现有页面；在日历、每日双栏和画廊原位置增加 skeleton/loading、空状态、局部错误和重试按钮。一个区域加载失败不能清空其他已成功区域，也不能显示旧数据为最新状态。

- [ ] **上传进度与恢复 UI**
  - 明确约束：复用上传按钮、进度文案和 modal，不新增独立页面；pending/uploading/finalizing/retrying 必须有不同文案与禁用状态。操作中禁止重复提交，失败后只开放安全的重试或关闭动作。

- [ ] **私有图片 URL 生命周期**
  - 明确约束：现有图片 DOM 不变；为 asset id 建立仅内存的 URL/过期时间缓存。过期自动刷新，同一 asset 避免并发重复签名；退出、删除或页面销毁时清除缓存，任何临时 URL 不进入持久化状态。

- [ ] **分页与懒加载**
  - 明确约束：月历只查当前月份，画廊按 cursor 追加，图片使用原生 lazy loading 或 IntersectionObserver；保留当前排序和卡片动效。切换月份要取消或忽略旧请求，防止旧响应覆盖新月份。

- [ ] **删除入口与状态**
  - 明确约束：在当前上传预览/画廊卡片内增加小型删除按钮和剩余时间提示，不改变整体布局；仅 owner 可见。删除中锁定按钮，超时后立即隐藏；对象已删但 complete RPC 失败时显示“继续完成删除”，不得重新上传覆盖。

- [ ] **服务端奖励余额**
  - 明确约束：背包卡片和数字动画保留；移除 `rewardRetroCards()`、`rewardedTens` 和本地余额写入。所有展示由奖励 RPC 返回或服务端账本查询结果生成，刷新页面后余额必须保持一致。

- [ ] **服务端补签事务**
  - 明确约束：保留现有“使用补签卡→选历史日期”的交互；选择只改变前端模式，不预扣卡。双方补签图片完成后调用 `completeRetroGlimmer()`，只有服务端返回 spent/already_completed 才更新 UI；余额不足时保持历史日期未完成展示。

- [ ] **服务端统计重算**
  - 明确约束：保留灯塔点亮、连续天数、金色天数和植物成长阈值视觉；输入数据只使用后端 ready、未删除且已计账记录。前端可计算展示派生值，但不得把计算结果写回作为业务真值。

- [ ] **消除用户内容 HTML 拼接**
  - 明确约束：重写 `renderUploadPreview()` 和 `renderGallery()` 的用户内容部分，结构和 CSS class 保持不变；固定模板可以保留，但用户 note、邮箱、显示名和错误必须通过文本节点赋值。

- [ ] **前端端到端测试**
  - 明确约束：测试必须在现有视觉页面上操作真实控件，覆盖注册/登录、session 恢复、双人上传互看、伪造角色失败、删除窗口、断网、过期 session、XSS 文本和 signed URL 刷新；测试数据使用固定前缀并在结束后清理，不能依赖 service-role key执行普通用户步骤。

## 3. 固定实施顺序

1. 登录/注册 UI、Supabase client 与 session 门禁。
2. Repository 初始化，替换 `localStorage` 业务数据源。
3. 当前月查询、私有图片读取和现有页面异步状态。
4. begin→upload→finalize、失败恢复和角色限定。
5. 删除/替换、奖励账本和补签事务。
6. 安全 DOM、分页懒加载、错误映射和浏览器端到端测试。
7. 确定正式域名后配置密码恢复 redirect URL，并用默认邮件服务做重置密码冒烟。
